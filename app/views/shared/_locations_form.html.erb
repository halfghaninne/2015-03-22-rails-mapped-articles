<%= f.fields_for :pins, newPin, child_index: ruby_object_id do |p| %>

	<%= p.label "new_location_alias", 
				"Name for location (visible to readers):" %>
	<br>
	<%= p.text_field("new_location_alias") %>

	<br><br>

	<%= p.label "new_location_address", "Address:" %>
	<br>
	<%= p.text_field("new_location_address", size: 50) %>
	
<% end %>
<br>
